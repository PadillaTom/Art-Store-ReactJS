export function isNativeModule({ binary, dependencies, devDependencies, files, gypfile }: {
    binary: any;
    dependencies?: {};
    devDependencies?: {};
    files?: any[];
    gypfile: any;
}): boolean;
